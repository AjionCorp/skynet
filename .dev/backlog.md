# Backlog

<!-- Priority: top = highest. Format: - [ ] [TAG] Task title — description -->
<!-- Markers: [ ] = pending, [>] = claimed by worker, [x] = done -->

- [>] [DATA] [DATA] Refresh blockers.md Active section to match current resolved state — all 5 Active items from 2026-02-20 (uncommitted working-tree changes, shell injection in config set, health score formula mismatch, stale heartbeat threshold hardcoding, 21 stale pending entries) have been resolved via subsequent commits on main. Move each to Resolved with fix references from .dev/completed.md. Update Mission Status to reflect 536+ completed tasks and 0 active failures. Mission: Criterion #4 trustworthy visibility and Criterion #5 measurable progress.
- [ ] [INFRA] [INFRA] Prune stale local dev/* branches from resolved failed tasks — run git branch, cross-reference each dev/ branch against .dev/failed-tasks.md status column. Delete branches whose entry is fixed or superseded via git branch -D. Skip branches matching any currently claimed [>] backlog task or active current-task-*.md worker. Emit branch_pruned events. Currently 19 stale branches. Mission: Criterion #3 clean state and Criterion #1 developer experience.
- [ ] [INFRA] [INFRA] Archive resolved failed-task rows to bound state file size — in scripts/watchdog.sh, verify or add _archive_old_failed_tasks() that moves fixed|superseded rows older than 48h from .dev/failed-tasks.md to .dev/failed-tasks-archive.md, keeping only the table header and most recent 20 resolved rows. Currently 267 rows with 0 active — main file should shrink to ~25 rows. Run pnpm typecheck. Mission: Criterion #3 bounded state and Criterion #2 fixer context reduction.
- [ ] [FIX] [FIX] Deduplicate repeated entries in completed.md — completed.md has ~25 duplicate task titles (e.g., Add shell completions for bash and zsh appears 14 times). In scripts/watchdog.sh _archive_old_completions(), add a dedup pass that collapses identical task titles keeping the earliest-date entry and removing 0m-duration duplicates. Run once to clean current state. Run pnpm typecheck. Mission: Criterion #5 accurate measurable progress and Criterion #3 clean state.
- [>] [FIX] [FIX] Extract duplicated mission evaluation logic from CLI status to shared module — in packages/cli/src/commands/status.ts, a TODO notes mission evaluation is duplicated from packages/dashboard/src/handlers/pipeline-status.ts. Create packages/dashboard/src/lib/mission-evaluator.ts exporting evaluateMissionProgress() and import from both. Delete local copies. Run pnpm typecheck. Mission: Criterion #3 maintainability and Criterion #5 consistent mission scoring.
- [>] [FIX] [FIX] Separate rate_limits write path from read-only DB access — in packages/dashboard/src/lib/db.ts, a TODO notes rate-limit writes are mixed with read-only access. Extract recordRateLimit/checkRateLimit into packages/dashboard/src/lib/rate-limiter.ts that imports the DB connection. Update callers. Run pnpm typecheck. Mission: Criterion #3 clean architecture and Criterion #1 safe adoption.
- [>] [TEST] [TEST] Add handler unit tests for mission-raw and pipeline-stream — create packages/dashboard/src/handlers/mission-raw.test.ts (reads mission.md, handles missing file, response shape) and packages/dashboard/src/handlers/pipeline-stream.test.ts (SSE headers, fs.watch mock, stream format, disconnect cleanup). Follow existing handler test patterns. These are the last 2 untested handlers. Run pnpm typecheck. Mission: Criterion #2 complete handler test coverage.
- [>] [INFRA] [INFRA] Complete echo agent dry-run lifecycle in scripts/agents/echo.sh — the stub has TODO: implement. In agent_run(), create a commit adding .dev/echo-proof-TASK_ID.md with the task description. In agent_check(), verify file exists and return success. Enables SKYNET_AGENT_PLUGIN=echo skynet start for pipeline testing without LLM tokens. Add bash -n validation. Mission: Criterion #1 faster setup and Criterion #6 multi-agent extensibility.
- [ ] [TEST] [TEST] Add integration test for watchdog crash recovery end-to-end — create tests/integration/crash-recovery.test.sh: create stale PID lock dir older than SKYNET_STALE_MINUTES, stale heartbeat, orphan worktree dir, and [>] claimed entry in test backlog. Run one watchdog cycle with SKYNET_ONE_SHOT=1. Assert lock cleaned, heartbeat removed, worktree pruned, claimed task unclaimed back to [ ]. Mission: Criterion #3 never lose tasks and Criterion #2 self-correction verification.
- [ ] [TEST] [TEST] Add Linux cron setup-agents regression test — create tests/unit/setup-agents-cron.test.sh that mocks uname to return Linux, exercises the cron path from setup-agents logic, and asserts crontab entries contain correct script paths, intervals matching SKYNET_WATCHDOG_INTERVAL, and log redirection. Currently only launchd is tested. Mission: Criterion #1 portability and Criterion #2 quality gates.
- [ ] [INFRA] [INFRA] Add task-fixer structured diagnostics for fix rate improvement — in scripts/task-fixer.sh, before each fix attempt log a JSON line to .dev/task-fixer-diagnostics.log with ts, task, error_type, attempt, branch, prior_error. After 3 failures and escalation to blocked, emit fix_approach_exhausted event with diagnostic history. Helps project-driver generate better retry tasks. Run pnpm typecheck. Mission: Criterion #2 self-correction improvement (fix rate 47%).
- [ ] [INFRA] [INFRA] Add pipeline idle detection and completion signaling to watchdog — in scripts/watchdog.sh, if pending backlog is 0, failed retries are 0, and no workers active for 3 consecutive cycles, emit pipeline_idle event and write .dev/pipeline-idle sentinel with timestamp. In packages/cli/src/commands/status.ts, show IDLE when sentinel exists. Prevents resource waste when mission is achieved. Mission: Criterion #5 measurable mission completion.

# Recent checked history (last 30)
- [x] [TEST] Add watchdog regression for active-root precedence and tie-break determinism — in `scripts/tests/watchdog.sh`, include fixtures where the same normalized root appears as `fixing-*`, `blocked`, and `pending`; assert canonical row selection follows precedence, unaffected rows are byte-identical, and second identical run is no-op. Mission: Criterion #2 quality gates and Criterion #3 convergent state.
- [x] [INFRA] Close stale-active failed-root drift in one deterministic watchdog sweep — in `scripts/watchdog.sh`, run a canonical active-root convergence pass that supersedes only `status=blocked|pending` rows whose normalized root is already present in `.dev/completed.md`, preserves `fixing-*` rows byte-for-byte, and emits before/after counters plus one stable root-hash summary. Mission: Criterion #2 self-correction throughput and Criterion #3 convergent state.
- [x] [INFRA] Prevent project-driver from generating retry variants for active failed roots — in `scripts/project-driver.sh`, normalize unchecked candidate titles against active `pending|blocked|fixing-*` roots in `.dev/failed-tasks.md`, skip generation when a canonical root is already active, and log deterministic `driver_duplicate_root_skipped` counters. Mission: Criterion #2 retry-loop reduction and Criterion #3 durable root-cause planning.
- [x] [DATA] Surface stale-active convergence counters in status surfaces — add `staleActiveCompletedRows` and `supersededActiveRows` from watchdog reconciliation to `packages/dashboard/src/handlers/pipeline-status.ts` and CLI `status --json` output, keeping field semantics aligned. Mission: Criterion #4 trustworthy visibility and Criterion #5 measurable progress.
- [x] [TEST] Add watchdog regression for deterministic stale-active sweep evidence — in `scripts/tests/watchdog.sh`, add a fixture with mixed `blocked|pending|fixing-*` rows where only completed-root stale active rows are superseded, unaffected rows remain byte-identical, and emitted counters/hash summary are stable across two identical runs. Mission: Criterion #2 quality gates and Criterion #3 deterministic recovery. _(typecheck failed)_
- [x] [TEST] Add watchdog regression for stale blocked/pending supersede guardrails — in `scripts/tests/watchdog.sh`, assert completed-root supersede touches only stale `blocked|pending` rows, never rewrites `fixing-*` rows, and preserves unaffected active-row ordering byte-for-byte. Mission: Criterion #2 quality gates and Criterion #3 deterministic recovery. _(typecheck failed)_
- [x] [INFRA] Canonicalize duplicate `status=blocked` root variants during watchdog reconciliation — in `scripts/watchdog.sh`, when multiple blocked rows share one normalized root and no `fixing-*` row owns that root, keep one deterministic canonical blocked row (latest row index tie-break, highest attempts preserved), mark lower-priority blocked variants `superseded`, and emit `blocked_duplicates_compacted` counters. Mission: Criterion #2 self-correction throughput and Criterion #3 one-root-one-active-row convergence.
- [x] [TEST] Add watchdog regression for blocked-duplicate compaction determinism — extend `scripts/tests/watchdog.sh` with fixtures containing repeated blocked rows per normalized root; assert one canonical blocked row survives, superseded rows preserve markdown validity/order, counters are stable, and second identical run is a no-op. Mission: Criterion #2 quality gates and Criterion #3 deterministic reconciliation.
- [x] [DATA] Surface blocked-duplication convergence counters in status JSON — extend `packages/dashboard/src/handlers/pipeline-status.ts` and `packages/cli/src/commands/status.ts` (`--json`) to include `blockedRowsBefore`, `blockedRowsAfter`, and `blockedDuplicatesCompacted` from watchdog telemetry for operator verification of blocked-root cleanup progress. Mission: Criterion #4 trustworthy visibility and Criterion #5 measurable progress.
- [x] [TEST] Add cross-surface parity regression for blocked compaction counters — extend `scripts/tests/watchdog.sh`, `packages/dashboard/src/handlers/pipeline-status.test.ts`, and CLI status JSON tests to prove `blockedRowsBefore/After` and `blockedDuplicatesCompacted` semantics stay aligned across watchdog logs and both status surfaces across two identical reconciliation passes. Mission: Criterion #2 quality gates and Criterion #4 telemetry trust.
- [x] [INFRA] Emit canonical watchdog reconciliation telemetry snapshot per cycle — in `scripts/watchdog.sh`, write `.dev/watchdog-telemetry.json` atomically after reconciliation with deterministic fields (`ts`, `activeRowsBefore`, `activeRowsAfter`, `parseGuardRows`, `staleActiveCompletedRows`, `supersededActiveRows`, `supersededByFixingRoot`, `blockedRowsBefore`, `blockedRowsAfter`, `blockedDuplicatesCompacted`, `canonicalizationPrecedenceApplied`) and skip rewrite when payload hash is unchanged. Mission: Criterion #4 trustworthy visibility and Criterion #3 deterministic state. _(worktree missing)_
- [x] [TEST] Add watchdog shell regression for telemetry snapshot determinism and no-op rewrites — extend `scripts/tests/watchdog.sh` to assert `.dev/watchdog-telemetry.json` is valid JSON, key-stable, idempotent across identical runs, and updated only when reconciliation counters change. Mission: Criterion #2 quality gates and Criterion #3 deterministic recovery. _(claude failed)_
- [x] [DATA] Read watchdog reconciliation counters from snapshot in dashboard status handler — update `packages/dashboard/src/handlers/pipeline-status.ts` to consume `.dev/watchdog-telemetry.json` as the primary source for reconciliation counters (including blocked-duplication fields) with a safe fallback to current behavior when the snapshot is missing. Mission: Criterion #4 telemetry trust and Criterion #5 measurable progress.
- [x] [DATA] Keep CLI `status --json` reconciliation counters aligned to watchdog snapshot semantics — update `packages/cli/src/commands/status.ts` to source the same watchdog snapshot fields as dashboard status and preserve field naming/meaning parity. Mission: Criterion #4 cross-surface consistency and Criterion #5 measurable mission progress. _(claude failed)_
- [x] [TEST] Add cross-surface parity regression for watchdog snapshot consumers — extend `packages/dashboard/src/handlers/pipeline-status.test.ts` and CLI status JSON tests to prove both surfaces return identical reconciliation and blocked-duplication counters from the same snapshot across two identical passes. Mission: Criterion #2 quality gates and Criterion #4 trustworthy operations. _(claude failed)_
- [x] [INFRA] Supersede stale `blocked|pending` rows when a `fixing-*` row already owns the same normalized root — in `scripts/watchdog.sh` reconciliation, when canonical precedence selects a `fixing-*` active row for a root, mark lower-priority active variants (`blocked|pending`) as `superseded` in the same pass and emit deterministic counters (`superseded_by_fixing_root`, `active_roots_after`). Mission: Criterion #2 retry-loop throughput and Criterion #3 one-root-one-active-row convergence.
- [x] [TEST] Add watchdog regression for fixing-root supersede behavior — in `scripts/tests/watchdog.sh`, add fixtures where the same normalized root appears as `fixing-*` plus `blocked|pending` rows and assert only the `fixing-*` row remains active, superseded rows preserve table validity/order, and second identical run is a no-op. Mission: Criterion #2 quality gates and Criterion #3 deterministic reconciliation.
- [x] [DATA] Surface parse-guard and fixing-root supersede counters in status JSON — extend `packages/dashboard/src/handlers/pipeline-status.ts` and `packages/cli/src/commands/status.ts` (`--json`) to include watchdog reconciliation counters (`parseGuardRows`, `supersededByFixingRoot`, `activeRowsBefore`, `activeRowsAfter`) so operators can verify convergence behavior from one status call. Mission: Criterion #4 trustworthy visibility and Criterion #5 measurable progress.
- [x] [INFRA] Enforce unchecked backlog hard-cap and canonical ordering in project-driver writes — in `scripts/project-driver.sh`, after task generation and before write, deterministically keep all claimed `[>]` rows first, then pending `[ ]`, trim lowest-priority pending rows beyond 15 unchecked total, and preserve checked history rows without mutation. Mission: Criterion #3 deterministic planning state and Criterion #2 no-task-loss reliability.
- [x] [TEST] Add project-driver regression for unchecked-cap + ordering invariants — in `scripts/tests/project-driver.sh`, fixture backlog with mixed `[>]`, `[ ]`, and misplaced `[x]` rows; assert claimed rows remain byte-identical and first, pending rows are capped at 15, checked history is moved to the bottom, and second identical run is no-op. Mission: Criterion #2 quality gates and Criterion #3 convergent planning behavior.
- [x] [INFRA] Auto-supersede stale `blocked` failed-task rows that match completed roots — in `scripts/watchdog.sh` reconciliation, extend completed-match supersede logic to `status=blocked` (not only pending variants), preserve `fixing-*` rows, and emit deterministic counters for `blocked_rows_superseded`. Mission: Criterion #2 retry-loop reduction and Criterion #3 convergent state.
- [x] [INFRA] Centralize failed-task markdown field codec into shared shell helpers — in `scripts/_config.sh`, add canonical encode/decode helpers for failed-task table fields (embedded pipes/backticks/newlines) and migrate `scripts/task-fixer.sh` plus `scripts/watchdog.sh` writers/parsers to one codec path. Mission: Criterion #3 deterministic state and Criterion #2 retry-loop stability.
- [x] [DATA] Surface `driver_low_fix_rate_mode` counters in status JSON outputs — extend `packages/dashboard/src/handlers/pipeline-status.ts` and `packages/cli/src/commands/status.ts` to include deterministic low-fix-rate counters (`activated`, `reasonFixRate`, `reasonRetryPressure`, `cappedNewTasks`) sourced from project-driver telemetry so operators can verify pressure behavior. Mission: Criterion #4 trustworthy visibility and Criterion #5 measurable progress.
- [x] [INFRA] Emit canonical project-driver telemetry snapshot per cycle — in `scripts/project-driver.sh`, write `.dev/project-driver-telemetry.json` atomically with deterministic fields (`ts`, `pendingBacklog`, `claimedBacklog`, `pendingRetries`, `fixRate`, `duplicateSkipped`, `maxNewTasks`, `driver_low_fix_rate_mode`) so downstream status surfaces can read one stable source of truth. Mission: Criterion #4 trustworthy visibility and Criterion #5 measurable progress.
- [x] [TEST] Add shell regression for project-driver telemetry snapshot determinism — in `scripts/tests/project-driver.sh`, assert telemetry JSON is valid, keys are stable, counters match fixture inputs, and identical inputs cause byte-identical output (or an explicit no-rewrite path) across two runs. Mission: Criterion #2 quality gates and Criterion #3 deterministic planning behavior. _(typecheck failed)_
- [x] [INFRA] Publish watchdog stale-active supersede skip diagnostics with throttle guards — in `scripts/watchdog.sh` reconciliation, emit deterministic reason codes/counts for rows not superseded (`status_fixing`, `no_completed_match`, `parse_guard`, `already_canonical`) plus bounded row samples so active-state drift can be triaged without log spam. Mission: Criterion #2 self-correction closure and Criterion #4 operational awareness.
- [x] [TEST] Add cross-script regression for shared failed-root normalization parity — extend `scripts/tests/project-driver.sh` and `scripts/tests/watchdog.sh` with the same adversarial title fixtures to prove canonical helpers produce identical normalized roots in dedupe and supersede flows. Mission: Criterion #2 quality gates and Criterion #3 deterministic recovery. _(claude failed)_
- [x] [INFRA] Compact duplicate non-active failed history rows before archive rotation — in `scripts/watchdog.sh`, collapse repeated `fixed|superseded` rows with the same normalized root+branch into one canonical history row before moving rows to `.dev/failed-tasks-archive.md`, preserving the newest attempts/error context and emitting deterministic `history_rows_compacted` metrics. Mission: Criterion #2 retry-loop throughput and Criterion #3 state convergence.
- [x] [TEST] Add regression for failed-task history compaction invariants — in `scripts/tests/watchdog.sh`, assert compaction never touches active rows, preserves markdown table validity, is idempotent across two runs, and keeps archive parity checks stable. Mission: Criterion #2 quality gates and Criterion #3 deterministic state management. _(typecheck failed)_
- [x] [TEST] Add project-driver regression for active-root duplicate suppression counters — in `scripts/tests/project-driver.sh`, assert generation skips unchecked candidates when normalized roots are already active in `.dev/failed-tasks.md`, never skips unrelated roots, and emits deterministic `driver_duplicate_root_skipped` totals. Mission: Criterion #2 quality gates and Criterion #3 convergent planning.
