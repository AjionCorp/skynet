# Backlog

<!-- Priority: top = highest. Format: - [ ] [TAG] Task title — description -->
<!-- Markers: [ ] = pending, [>] = claimed by worker, [x] = done -->

- [ ] [INFRA] Fix mission state stuck at "active" — .dev/mission.md still says State: active despite all 10 goals and 8 success criteria being checked. Source scripts/_config.sh and scripts/mission-state.sh, then call mission_set_state to transition to complete. Also call mission_write_completion_summary to write the summary file. Verify State: complete in mission.md after running.
- [ ] [INFRA] Create mission lifecycle directories — .dev/completed-missions/ (for archived mission summaries) and .dev/mission-queue/ (for next-mission sequencing) do not exist. Create both directories, add .gitkeep files, and update mission_write_completion_summary() in scripts/mission-state.sh to write to completed-missions/ instead of flat .dev/. Update watchdog.sh mission-queue scan path if needed.
- [ ] [TEST] Add unit test for watchdog dispatch loop — scripts/watchdog.sh is the main pipeline orchestrator with no direct unit tests. In tests/unit/, create watchdog.test.sh that sources watchdog helper functions and asserts correct behavior for: stale lock detection, worker dispatch thresholds, and completion archival triggers.
- [ ] [TEST] Add handler test for /api/admin/config route — In packages/admin/src/app/api/admin/config/, create route.test.ts using the existing vitest mock pattern (vi.hoisted + vi.mock for handler factory and skynet-config). Assert GET delegates to createConfigHandler, exports dynamic force-dynamic, and returns expected { data, error } shape.
- [ ] [TEST] Add handler test for /api/admin/events route — In packages/admin/src/app/api/admin/events/, create route.test.ts using the vitest factory mock pattern. Assert GET delegates to createEventsHandler, exports dynamic force-dynamic, and returns expected response shape.
- [ ] [INFRA] Prune stale .dev sentinel files — .db-backup-sentinel-* and .db-vacuum-sentinel-* files accumulate without cleanup (currently 10 files). Add a cleanup step to watchdog.sh (alongside existing _archive_old_completions) that removes sentinel files older than 7 days. One function, one find call, called once per watchdog cycle.
- [>] [INFRA] Add watchdog pruning for mission-complete sentinel files — Mission-complete sentinel files (.dev/mission-complete-*) persist indefinitely after missions finish. Add a cleanup function to watchdog.sh that removes mission-complete sentinel files older than 30 days, called alongside existing _archive_old_completions.
- [ ] [INFRA] Archive resolved blockers older than 30 days from blockers.md — .dev/blockers.md has 40+ resolved entries dating back to 2026-02-19. Move resolved entries older than 30 days to .dev/blockers-archive.md to keep the active file concise. Can be a one-time cleanup or a recurring watchdog function.
