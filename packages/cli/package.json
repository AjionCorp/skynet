{
  "name": "@ajioncorp/skynet-cli",
  "version": "0.1.0",
  "type": "module",
  "files": [
    "dist",
    "scripts",
    "templates",
    "README.md"
  ],
  "bin": {
    "skynet": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prepack": "cp -r ../../scripts ./scripts && cp -r ../../templates ./templates",
    "postpack": "rm -rf ./scripts ./templates",
    "prepublishOnly": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
