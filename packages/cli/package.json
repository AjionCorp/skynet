{
  "name": "@ajioncorp/skynet-cli",
  "version": "0.1.0",
  "description": "CLI for Skynet — autonomous AI development pipeline",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AjionCorp/skynet"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "skynet",
    "ai",
    "pipeline",
    "claude",
    "autonomous"
  ],
  "files": [
    "dist",
    "scripts",
    "templates",
    "README.md"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "skynet": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prepack": "test -d ../../scripts && test -d ../../templates && cp -r ../../scripts ./scripts && cp -r ../../templates ./templates",
    "postpack": "rm -rf ./scripts ./templates; true",
    "prepublishOnly": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@ajioncorp/skynet": "workspace:*",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
